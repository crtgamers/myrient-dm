/**
 * Test placeholder para que `npm run test:integration` tenga al menos un test.
 */
describe('placeholder integration', () => {
  it('debería pasar para validar que Jest encuentra tests de integración', () => {
    expect(true).toBe(true);
  });
});
