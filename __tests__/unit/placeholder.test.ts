/**
 * Test placeholder para que `npm run test:unit` tenga al menos un test.
 */
describe('placeholder unit', () => {
  it('debería pasar para validar que Jest está configurado', () => {
    expect(true).toBe(true);
  });
});
