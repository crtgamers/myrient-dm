/**
 * @fileoverview Punto de entrada de tipos compartidos entre proceso principal (Electron) y renderer (Vue).
 * @module shared/types
 *
 * Centraliza la reexportación de tipos usados en IPC, servicios y UI para mantener una única definición.
 */

export type { SearchOptions } from './search';
